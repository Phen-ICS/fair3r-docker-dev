CKAN_VERSION=2.11.5

#dev or prod
CKAN_DEBUG=false
CKAN_PORT=5000

# Nginx
NGINX_SSLPORT_HOST=8085
NGINX_SSLPORT=443

# DB
CKAN_DB_PORT_EXT=5435

CKAN_SITE_URL=http://localhost:5000
CKAN_INTERNAL_SITE_URL=http://ckan:5000

CKAN_DB_HOST=db
CKAN_DB_PORT=5432
CKAN_DB_NAME=ckan
CKAN_DB_USER=ckan
CKAN_DB_PASSWORD=ckan

CKAN_DATASTORE_DB_NAME=datastore
CKAN_DATASTORE_DB_USER=ckan_datastore
CKAN_DATASTORE_DB_PASSWORD=ckan_datastore
CKAN_DATASTORE_READONLY_USER=ckan_datastore_ro
CKAN_DATASTORE_READONLY_PASSWORD=ckan_datastore_ro

# Test DB names
CKAN_TEST_DB_NAME=ckan_test
CKAN_DATASTORE_TEST_DB_NAME=datastore_test

# complete these for deployments
CKAN_SESSION_SECRET=change-me-session-secret
CKAN_SECRET_KEY=change-me-secret-key
CKAN_APP_INSTANCE_UUID=change-me-app-instance-uuid
CKAN_BOOTSTRAP_SYSADMIN_NAME=
CKAN_BOOTSTRAP_SYSADMIN_EMAIL=
CKAN_BOOTSTRAP_SYSADMIN_PASSWORD=

# add each extension to the list of plugins to be installed and activated (ie: space-separated list of extension names)
CKAN_EXTRA_PLUGINS=fair3r doi pages plotly_explorer
CKAN_EXTRA_VIEWS=plotly_explorer

# Options for extensions
# Fair3R (use upper-case; native VM deploys set ckanext.fair3r.context to VALIDATION / INTEGRATION / PRODUCTION)
FAIR3R_CONTEXT=DEV
FAIR3R_ENABLE_FDF_INTEGRATION=true
# DEV only: absolute path on the host to a clone of
# https://github.com/Phen-ICS/fair3r-fdf-schema (directory that contains fdf_schema.json).
# When set, Fair3R reads the schema from that mount instead of downloading GitHub.
# Leave empty to download GitHub main at container start (default for quick DEV setup).
# Example: FDF_SCHEMA_LOCAL_PATH=/home/you/src/fair3r-fdf-schema
FDF_SCHEMA_LOCAL_PATH=

# Upload limit (MB) for resource files. CKAN defaults to 10 when left
# unset. nginx's client_max_body_size is already well above this in every
# environment (100-140M), so it's never the binding constraint.
CKAN_MAX_RESOURCE_SIZE=100

# Contact
CONTACT_MAIL=contact@example.com

# DOI
DOI_ACCOUNT_NAME=
DOI_ACCOUNT_PASSWORD=
DOI_PREFIX=
DOI_PUBLISHER=
DOI_TEST_MODE=True
DOI_SITE_TITLE=Fair3R

# SMTP / Email
CKAN_EMAIL_SMTP_SERVER=smtp.example.com:587
CKAN_EMAIL_SMTP_STARTTLS=true
CKAN_EMAIL_SMTP_STARTTLS_VERIFY=false
CKAN_EMAIL_SMTP_USER=
CKAN_EMAIL_SMTP_PASSWORD=
CKAN_EMAIL_SMTP_MAIL_FROM=
CKAN_EMAIL_SMTP_REPLY_TO=
CKAN_EMAIL_SMTP_EMAIL_TO=
CKAN_EMAIL_SMTP_ERROR_EMAIL_FROM=